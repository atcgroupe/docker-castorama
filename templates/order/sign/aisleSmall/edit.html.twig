{% extends 'core/core.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row mb-5 pt-5">
            <div class="col">
                <h3 class="text-primary mb-3">Panneau allée New</h3>

                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                    <h4 class="mt-5 mb-3">Personnalisation</h4>

                    <div class="mb-4">
                        {{ form_row(form.aisleNumber) }}
                    </div>

                    <div class="mb-4">
                        {{ form_row(form.category) }}
                    </div>

                    <div class="mb-4">
                        {{ form_row(form.item1) }}
                    </div>

                    <div id="aisle-item2-container" class="item-container mb-4">
                        {{ form_row(form.item2) }}
                    </div>

                    <div id="aisle-item3-container" class="item-container mb-4">
                        {{ form_row(form.item3) }}
                    </div>

                    <div class="mb-4">
                        {{ form_row(form.quantity)}}
                    </div>

                    {% include 'order/sign/_edit_save.html.twig' %}

                {{ form_end(form) }}
            </div>
            <div class="col">
                <h5 class="text-primary">Recto</h5>

                <div>
                    {% include 'order/sign/aisleSmall/template.html.twig' with
                        {
                            'id': 1,
                            'scale': 'medium',
                            'face': 'recto',
                            'number': '',
                            'text_line_1': '',
                            'text_line_2': '',
                            'text_line_3': ''
                        }
                    %}
                </div>

                <h5 class="mt-3 text-primary">Verso</h5>

                <div>
                    {% include 'order/sign/aisleSmall/template.html.twig' with
                        {
                            'id': 1,
                            'scale': 'medium',
                            'face': 'verso',
                            'number': '',
                            'text_line_1': '',
                            'text_line_2': '',
                            'text_line_3': ''
                        }
                    %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('aisleSignEdit') }}
{% endblock %}
