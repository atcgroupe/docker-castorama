{% extends 'core/core.html.twig' %}

{% block content %}
    <div class="row justify-content-lg-center">
        <div class="col-lg-6">
            <h1 class="h2 fw-light text-center my-5">Nouvelle saisie</h1>

            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(form) }}

                <div class="mt-5">
                    <button type="submit" class="btn btn-outline-primary w-100">Créer</button>
                </div>

                <div class="my-3 text-center">
                    <a href="{{ path('orders_list_active') }}" class="text-muted">Annuler</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
